# Architecture

Start with [`system-overview.md`](./system-overview.md).

- `master_erd.mermaid` — authoritative data model (consolidated from all design docs).
- `Auth_System_Design.md` — authentication, OTP, JWT, identity scheme, RLS handoff.
- `*_Dashboard_System_Design.md` — per-role surfaces, status models, data model, RLS policies.

Decisions are recorded in [`../adr/`](../adr/). Requirements live in [`../specs/`](../specs/).
